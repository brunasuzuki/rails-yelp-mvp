<%= form_with(model: exemplo) do |form| %>
  <% if exemplo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exemplo.errors.count, "error") %> prohibited this exemplo from being saved:</h2>

      <ul>
        <% exemplo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
